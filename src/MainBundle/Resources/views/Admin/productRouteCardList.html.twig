{% extends 'SonataAdminBundle:CRUD:base_list.html.twig' %}

{% set price = 0 %}

 {% block table_body %}
     <tbody>
     {% for object in admin.datagrid.results %}

         {% set routeCardPrice = object.routeCardPrice %}

         {% set price = price + routeCardPrice %}

         <tr>
             {% include admin.getTemplate('inner_list_row') %}
         </tr>
     {% endfor %}

     <tr>
         <td colspan="12">
             {{ "Ընդամենը" }}
         </td>
         <td colspan="3">
             {{ price }}
         </td>
     </tr>

     </tbody>

 {% endblock %}