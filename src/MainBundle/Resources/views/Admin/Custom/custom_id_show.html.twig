{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}

{%- block field %}
    {% set listIds = app.request.get('listIds') %}

    {% for data in listIds %}
        {% if data.id == value %}
            {{ data.row }}
        {% endif %}
    {% endfor %}
{% endblock -%}