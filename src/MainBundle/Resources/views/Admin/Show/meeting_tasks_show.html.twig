{% extends 'SonataAdminBundle:CRUD:base_show_field.html.twig' %}

{% block field %}
    <ul>
        {% for name in value %}
            <li>
                {{ name.description|slice(0, 100) }}
            </li>
        {% endfor %}
    </ul>
{% endblock %}

