{% extends 'SonataAdminBundle:CRUD:base_list_field.html.twig' %}

{%- block field %}

    {% if object.id %}
        {% set chronology = object.getToolsChronology() %}

        {% if chronology|length > 0 %}
            {% set url = '/admin/tools_chronology/list?toolId=' ~ object.id %}

            <a href="{{ url }}" title="{{ 'history'|trans() }}" target="_blank">
                {{ 'history'|trans() }}
            </a>
        {% endif %}
    {% endif %}
{% endblock -%}




